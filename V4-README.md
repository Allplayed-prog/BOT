# Nythera V4

1. `supabase/Nythera-Komplett-V4.sql` vollständig in Supabase SQL Editor ausführen.
2. Bot-Dateien in GitHub hochladen und ersetzen.
3. Railway neu deployen.
4. `/safety` ausführen.
5. Danach `/setup`, `/register`, `/bloodin`, `/bloodout`, `/event` und `/panel` testen.

Dieser Bot behandelt Panel-IDs als UUID und Registrierungen mit discord_id plus discord_user_id.
